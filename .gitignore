# Confidential client briefs — never publish to the public Pages repo
*.docx
~$*

# Local preview / tooling config
.claude/

# Local extraction / scratch artifacts
_*.txt
_*.py

# Comparison drafts (e.g. Gemini's version) — not part of the deliverable
preview/
