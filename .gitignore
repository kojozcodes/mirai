# Confidential client briefs — never publish to the public Pages repo
*.docx
~$*

# Local preview / tooling config
.claude/

# Local extraction / scratch artifacts
_*.txt
_*.py

# temp: downloaded reference source
_ref/

# HubSpot CLI auth (contains a personal access key — never commit)
hubspot.config.yml
.hubspot/
